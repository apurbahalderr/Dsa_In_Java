class Solution {
    public boolean stoneGame(int[] piles) {
        //as alice is choosing first so she can compute whether odd indexed value is greater or even indexed value is greater
        //then she can force bob to take the smaller indexed pile and can win always
        return true;
    }
}